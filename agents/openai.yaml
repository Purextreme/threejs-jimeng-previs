interface:
  display_name: "Three.js 即梦白模预演"
  short_description: "复用统一预演 UI，并强制多帧视觉验收和 MP4 校验"
  default_prompt: "Use $threejs-jimeng-previs to build a reusable white-model previs and validate its sampled frames and MP4."
